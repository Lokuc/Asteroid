using UnityEngine;

namespace UI
{
    public class CameraSett : MonoBehaviour
    {
        // Start is called before the first frame update

        // private Camera cam;
        // private int he, we;
        // private float time;
        // private bool change;
        void Start()
        {
            // cam = GetComponent<Camera>();
            // he = cam.pixelHeight;
            // we = cam.pixelWidth;
            // change = false;
        }

        // Update is called once per frame
        void Update()
        {
            // if (change)
            // {
            //     time += Time.deltaTime;
            //     if (time > 0.5f)
            //     {
            //         he = cam.pixelHeight;
            //         we = cam.pixelWidth;
            //         Screen.SetResolution((int)(he * 1.777777f),he,false);
            //         change = false;
            //     }
            // }
            // else
            // {
            //     if (cam.pixelHeight != he || cam.pixelWidth != we)
            //     {
            //         change = true;
            //         time = 0f;
            //     }
            // }

        }
    }
}
